# @google:registry=https://wombat-dressing-room.appspot.com
# 注释掉 wombat 配置，使用公共 npm registry
# 如果需要在 Google 内网环境使用，取消上面的注释并运行 npm run auth

# 为 @google/genai 单独配置使用公共 registry（如果其他 @google/* 包需要 wombat）
# @google/genai:registry=https://registry.npmjs.org/